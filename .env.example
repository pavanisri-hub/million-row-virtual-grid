# Environment variables for million-row-virtual-grid

# Currently, the application does not require any mandatory environment variables.
# This file exists to satisfy the submission checklist and for future extension.

# Example placeholders:
# API_BASE_URL=http://localhost:3000
# NODE_ENV=development
